name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Publish Fewshot.Api
        run: |
          dotnet publish src/Fewshot.Api/Fewshot.Api.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -o publish/api

      - name: Publish Fewshot.Mcp
        run: |
          dotnet publish src/Fewshot.Mcp/Fewshot.Mcp.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=true `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -o publish/mcp

      - name: Publish Fewshot.Dashboard
        run: |
          dotnet publish src/Fewshot.Dashboard/Fewshot.Dashboard.csproj `
            -c Release `
            -o publish/dashboard

      - name: Zip artifacts
        run: |
          Compress-Archive -Path publish/api/*       -DestinationPath publish/Fewshot.Api-win-x64.zip
          Compress-Archive -Path publish/mcp/*       -DestinationPath publish/Fewshot.Mcp-win-x64.zip
          Compress-Archive -Path publish/dashboard/* -DestinationPath publish/Fewshot.Dashboard.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Fewshot ${{ github.ref_name }}
          body: |
            ## Install

            **One-line installer (PowerShell):**
            ```powershell
            irm https://raw.githubusercontent.com/fewshot-app/fewshot/main/install.ps1 | iex
            ```

            **Requirements:** [Ollama](https://ollama.com) + .NET 8 (auto-installed if missing)

            ## What's included
            - `Fewshot.Api-win-x64.zip` — API server (self-contained, no .NET install needed)
            - `Fewshot.Mcp-win-x64.zip` — MCP stdio server for Claude Desktop
            - `Fewshot.Dashboard.zip` — Blazor WASM dashboard (static files)

            ## Manual install
            See [README](https://github.com/fewshot-app/fewshot/blob/main/README.md) for manual setup instructions.
          files: |
            publish/Fewshot.Api-win-x64.zip
            publish/Fewshot.Mcp-win-x64.zip
            publish/Fewshot.Dashboard.zip
          draft: false
          prerelease: false
